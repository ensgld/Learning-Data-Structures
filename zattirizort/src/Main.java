// Press Shift twice to open the Search Everywhere dialog and type `show whitespaces`,
// then press Enter. You can now see whitespace characters in your code.
public class Main {
    public static void main(String[] args) {
        String satir = "TURKIYE 85.000.000 Ankara Istanbul Turkish TRY";
        String str="S";
        String str1="s";
        System.out.println(str.toLowerCase().equals(str1.toLowerCase()));





       /*
        String countryName=satir.substring(0,satir.indexOf(" "));
          System.out.println(countryName);
        int countryStartIndex= satir.indexOf(countryName);

        System.out.println(countryStartIndex);
        String population = satir.substring(satir.indexOf(" ") + 1, satir.indexOf(" ", satir.indexOf(" ") + 1));
        population = population.replace(".", "");
        System.out.println(population);
        String capitalCity = satir.substring(satir.indexOf(countryName.length()+population.length()),satir.indexOf());
        System.out.println(capitalCity);
        */

    }
}